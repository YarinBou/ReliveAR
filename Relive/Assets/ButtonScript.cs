using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ButtonScript : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
       
    }

    //void enabledButton(bool enabled)
    //{ }
    //    this.interactable = false;
    //    this.transform.enabled = false;
    //    restartButton.GetComponent<Button>().enabled = false;
    //    restartButton.GetComponent<UnityEngine.UI.Image>().enabled = false;
    //    restartButton.transform.GetChild(0).GetComponent<Text>().enabled = false;
    
}
